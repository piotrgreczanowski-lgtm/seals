<!DOCTYPE html>
<html lang="pl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Mates Barber Shop - Premium Barber we Wrocławiu</title>
    
    <!-- Favicon updated to use the correct, direct image link -->
    <link rel="icon" href="https://i.ibb.co/BVRZJ5sC/316565714-522190629926253-6172686213198234951-n.jpg$0">

    <!-- Google Fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;600;700&family=Playfair+Display:wght@800&display=swap" rel="stylesheet">

    <style>
        /* ==============================================
        ROOT VARIABLES & GLOBAL STYLES - FINAL THEME
        ==============================================
        */
        :root {
            /* Background color now perfectly matches the logo's background */
            --bg-color: #e9e7e2; 
            --surface-color: #FFFFFF;
            --primary-accent: #3A4F4D; /* Dark Teal from logo */
            --text-primary: #1C2827;   /* Darker Teal for readability */
            --text-secondary: #5E706E; /* Muted Teal for secondary text */
            --border-color: rgba(0, 0, 0, 0.1);
            
            --font-body: 'Inter', sans-serif;
            --font-display: 'Playfair Display', serif;
            
            --transition-fast: 0.2s ease;
            --transition-medium: 0.4s ease;
        }

        *, *::before, *::after {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        html {
            scroll-behavior: smooth;
            font-size: 16px;
        }

        body {
            background-color: var(--bg-color);
            color: var(--text-primary);
            font-family: var(--font-body);
            line-height: 1.7;
            -webkit-font-smoothing: antialiased;
            -moz-osx-font-smoothing: grayscale;
        }

        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 2rem;
        }

        section {
            padding: 8rem 0;
        }

        .section-title {
            font-family: var(--font-display);
            font-size: clamp(2.5rem, 5vw, 4rem);
            text-align: center;
            margin-bottom: 4rem;
            color: var(--text-primary);
            font-weight: 800;
        }
        
        .reveal {
            opacity: 0;
            transform: translateY(30px);
            transition: opacity 0.8s ease-out, transform 0.6s ease-out;
        }

        .reveal.visible {
            opacity: 1;
            transform: translateY(0);
        }

        /* ==============================================
        HEADER & NAVIGATION
        ==============================================
        */
        .header {
            padding: 1.5rem 0;
            position: fixed;
            width: 100%;
            z-index: 1000;
            transition: background-color var(--transition-medium), padding var(--transition-medium);
        }
        
        .header.scrolled {
            /* Updated to match the new background color */
            background-color: rgba(233, 231, 226, 0.8); 
            -webkit-backdrop-filter: blur(10px);
            backdrop-filter: blur(10px);
            border-bottom: 1px solid var(--border-color);
            padding: 1rem 0;
        }

        .navbar {
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .logo a {
            display: flex;
            align-items: center;
            text-decoration: none;
        }
        
        .logo img {
            /* Logo size increased */
            height: 60px;
            width: auto;
        }

        .nav-links a {
            color: var(--text-secondary);
            text-decoration: none;
            margin-left: 2.5rem;
            font-weight: 600;
            font-size: 0.9rem;
            letter-spacing: 0.5px;
            position: relative;
            transition: color var(--transition-fast);
        }

        .nav-links a:hover {
            color: var(--text-primary);
        }

        /* ==============================================
        HERO SECTION
        ==============================================
        */
        .hero {
            height: 100vh;
            min-height: 700px;
            display: flex;
            align-items: center;
            text-align: left;
            /* Updated gradient to match the new background color */
            background: linear-gradient(90deg, rgba(233, 231, 226, 0.95) 0%, rgba(233, 231, 226, 0.7) 50%, rgba(233, 231, 226, 0) 100%), url('https://images.unsplash.com/photo-1567894340345-a63442149104?q=80&w=2940&auto=format&fit=crop') no-repeat center center/cover;
            padding-top: 80px;
        }

        .hero-content {
            max-width: 650px;
        }

        .hero-content h1 {
            font-family: var(--font-display);
            font-size: clamp(3.5rem, 6vw, 6rem);
            margin-bottom: 1.5rem;
            line-height: 1.1;
            color: var(--text-primary);
            font-weight: 800;
        }

        .hero-content p {
            font-size: 1.1rem;
            max-width: 500px;
            margin-bottom: 2.5rem;
            font-weight: 400;
            color: var(--text-secondary);
        }

        /* ==============================================
        BUTTONS
        ==============================================
        */
        .btn {
            display: inline-block;
            padding: 1rem 2.5rem;
            background-color: var(--primary-accent);
            color: var(--surface-color);
            border: 1px solid var(--primary-accent);
            border-radius: 5px;
            text-decoration: none;
            font-weight: 700;
            cursor: pointer;
            transition: background-color var(--transition-fast), color var(--transition-fast), transform var(--transition-fast);
            text-align: center;
        }

        .btn:hover {
            background-color: transparent;
            color: var(--primary-accent);
            transform: translateY(-3px);
        }
        
        .btn-secondary {
            background-color: transparent;
            color: var(--primary-accent);
        }
        
        .btn-secondary:hover {
             background-color: var(--primary-accent);
             color: var(--surface-color);
        }

        /* ==============================================
        PRICE LIST & REVIEWS SECTION
        ==============================================
        */
        #cennik {
             background-color: var(--bg-color);
        }
        
        #opinie {
            background-color: var(--surface-color);
        }
        
        .price-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 2rem;
        }

        .price-card {
            background-color: var(--surface-color);
            padding: 2rem;
            border: 1px solid var(--border-color);
            border-radius: 8px;
            transition: transform var(--transition-medium), box-shadow var(--transition-medium), border-color var(--transition-medium);
            cursor: pointer;
            box-shadow: 0 4px 15px rgba(0,0,0,0.05);
        }

        .price-card:hover {
            transform: translateY(-10px);
            border-color: var(--primary-accent);
            box-shadow: 0 15px 30px rgba(0, 0, 0, 0.1);
        }

        .price-card-header {
            display: flex;
            justify-content: space-between;
            align-items: baseline;
            margin-bottom: 1rem;
            border-bottom: 1px solid var(--border-color);
            padding-bottom: 1rem;
        }

        .price-card-title {
            font-size: 1.3rem;
            font-weight: 600;
            color: var(--text-primary);
        }

        .price-card-value {
            font-size: 1.5rem;
            font-weight: 700;
            color: var(--primary-accent);
        }
        
        .price-card-duration {
            font-size: 0.9rem;
            color: var(--text-secondary);
        }

        .reviews-content {
            display: flex;
            flex-direction: column;
            align-items: center;
            gap: 2rem;
            background-color: var(--bg-color);
            padding: 4rem;
            border-radius: 8px;
        }

        .rating-summary {
            display: flex;
            align-items: center;
            gap: 1.5rem;
            flex-wrap: wrap;
            justify-content: center;
        }

        .rating-value {
            font-size: 4rem;
            font-weight: 800;
            font-family: var(--font-display);
            line-height: 1;
            color: var(--text-primary);
        }

        .rating-stars {
            display: flex;
            flex-direction: column;
        }

        .stars {
            display: flex;
            gap: 0.25rem;
            color: #D4AF37;
        }

        .stars svg {
            width: 28px;
            height: 28px;
        }

        .rating-count {
            font-size: 1rem;
            color: var(--text-secondary);
        }

        /* ==============================================
        BOOKING SECTION
        ==============================================
        */
        #rezerwacja {
            background-color: var(--surface-color);
        }
        
        .booking-form {
            max-width: 700px;
            margin: 0 auto;
            padding: 3rem;
            background-color: var(--surface-color);
            border: 1px solid var(--border-color);
            border-radius: 8px;
            box-shadow: 0 4px 15px rgba(0,0,0,0.05);
        }

        .form-group {
            margin-bottom: 2rem;
        }

        .form-group label {
            display: block;
            margin-bottom: 0.75rem;
            font-weight: 600;
            font-size: 0.9rem;
            color: var(--text-secondary);
            text-transform: uppercase;
            letter-spacing: 1px;
        }

        .form-group select,
        .form-group input[type="date"] {
            width: 100%;
            padding: 1rem;
            background-color: var(--bg-color);
            border: 1px solid var(--border-color);
            border-radius: 5px;
            color: var(--text-primary);
            font-family: var(--font-body);
            font-size: 1rem;
            transition: border-color var(--transition-fast);
        }
        
        .form-group select:focus,
        .form-group input[type="date"]:focus {
            outline: none;
            border-color: var(--primary-accent);
        }
        
        input[type="date"]::-webkit-calendar-picker-indicator {
            filter: none;
            cursor: pointer;
        }

        #time-slots {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(100px, 1fr));
            gap: 0.75rem;
            margin-top: 1rem;
        }

        .time-slot {
            padding: 0.8rem;
            border: 1px solid var(--border-color);
            color: var(--text-secondary);
            background-color: var(--bg-color);
            border-radius: 5px;
            text-align: center;
            cursor: pointer;
            transition: all var(--transition-fast);
        }
        
        .time-slot.selected,
        .time-slot:hover {
            background-color: var(--primary-accent);
            color: var(--surface-color);
            border-color: var(--primary-accent);
            transform: scale(1.05);
        }

        .closed-message {
            text-align: center;
            padding: 2rem;
            color: var(--text-secondary);
            background-color: var(--bg-color);
            border: 1px dashed var(--border-color);
            border-radius: 5px;
        }

        /* ==============================================
        FOOTER
        ==============================================
        */
        .footer {
            background-color: var(--surface-color);
            padding: 5rem 0;
            text-align: center;
            border-top: 1px solid var(--border-color);
        }

        .footer-content {
             display: flex;
             flex-direction: column;
             align-items: center;
             gap: 2.5rem;
        }

        .footer-info-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 2rem;
            width: 100%;
            max-width: 800px;
            text-align: left;
        }

        .info-block h4 {
            font-size: 1rem;
            font-weight: 600;
            color: var(--text-primary);
            margin-bottom: 1rem;
            text-transform: uppercase;
            letter-spacing: 1px;
            border-bottom: 1px solid var(--border-color);
            padding-bottom: 0.5rem;
        }

        .info-block p, .info-block a {
            color: var(--text-secondary);
            text-decoration: none;
            transition: color var(--transition-fast);
            line-height: 1.8;
        }

        .info-block a:hover {
            color: var(--primary-accent);
        }
        
        .social-links {
            display: flex;
            gap: 1rem;
            margin-top: 1rem;
        }
        
        .social-links a svg {
            width: 24px;
            height: 24px;
            stroke: var(--text-secondary);
            transition: stroke var(--transition-fast);
        }
        
        .social-links a:hover svg {
            stroke: var(--primary-accent);
        }

        .footer-bottom {
            margin-top: 3rem;
            width: 100%;
            border-top: 1px solid var(--border-color);
            padding-top: 2rem;
            display: flex;
            justify-content: space-between;
            align-items: center;
            font-size: 0.9rem;
            color: var(--text-secondary);
        }
        
        .footer-bottom a {
            color: var(--text-secondary);
            text-decoration: none;
            transition: color var(--transition-fast);
        }
        
        .footer-bottom a:hover {
            color: var(--text-primary);
        }

        /* ==============================================
        CUSTOM MODAL
        ==============================================
        */
        .modal-overlay {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(10, 10, 10, 0.5);
            -webkit-backdrop-filter: blur(5px);
            backdrop-filter: blur(5px);
            display: flex;
            justify-content: center;
            align-items: center;
            z-index: 2000;
            opacity: 0;
            visibility: hidden;
            transition: opacity var(--transition-medium), visibility var(--transition-medium);
        }

        .modal-overlay.visible {
            opacity: 1;
            visibility: visible;
        }

        .modal-box {
            background: var(--surface-color);
            padding: 3rem;
            border-radius: 8px;
            border: 1px solid var(--border-color);
            max-width: 600px;
            width: 90%;
            transform: scale(0.95);
            transition: transform var(--transition-medium);
            box-shadow: 0 10px 30px rgba(0,0,0,0.1);
        }
        
        .modal-overlay.visible .modal-box {
            transform: scale(1);
        }

        .modal-box h3 {
            font-family: var(--font-display);
            font-size: 1.8rem;
            margin-bottom: 1rem;
            color: var(--text-primary);
        }

        .modal-box p {
            color: var(--text-secondary);
            margin-bottom: 2rem;
            line-height: 1.6;
            text-align: left;
        }

        /* ==============================================
        RESPONSIVE DESIGN
        ==============================================
        */
        @media (max-width: 768px) {
            .nav-links {
                display: none; /* For simplicity. */
            }
            .hero {
                text-align: center;
                /* Updated gradient to match the new background color */
                background: linear-gradient(rgba(233, 231, 226, 0.85), rgba(233, 231, 226, 0.85)), url('https://images.unsplash.com/photo-1567894340345-a63442149104?q=80&w=2940&auto=format&fit=crop') no-repeat center center/cover;
            }
            .hero-content {
                margin: 0 auto;
            }
            .footer-bottom {
                flex-direction: column;
                gap: 1rem;
            }
        }
    </style>
</head>
<body>

    <!-- Header -->
    <header class="header" id="header">
        <div class="container">
            <nav class="navbar">
                <div class="logo">
                     <a href="#">
                        <!-- Logo updated to use the correct, direct image link -->
                        <img src="https://i.ibb.co/BVRZJ5sC/316565714-522190629926253-6172686213198234951-n.jpg$0" alt="Mates Barber Shop Logo">
                    </a>
                </div>
                <div class="nav-links">
                    <a href="#cennik">Usługi</a>
                    <a href="#opinie">Opinie</a>
                    <a href="#rezerwacja">Rezerwacja</a>
                    <a href="#kontakt">Kontakt</a>
                </div>
            </nav>
        </div>
    </header>

    <main>
        <!-- Hero Section -->
        <section class="hero">
            <div class="container">
                <div class="hero-content reveal">
                    <h1>Precyzja w Każdym Cięciu.</h1>
                    <p>Odkryj rzemiosło barberskie na najwyższym poziomie. W Mates Barber Shop dbamy o każdy detal, aby Twoja wizyta była wyjątkowym doświadczeniem.</p>
                    <a href="#rezerwacja" class="btn">Umów Wizytę</a>
                </div>
            </div>
        </section>

        <!-- Price List Section -->
        <section id="cennik">
            <div class="container">
                <h2 class="section-title reveal">Nasze Usługi</h2>
                <div class="price-grid">
                    <div class="price-card reveal" data-service-value="Strzyzenie">
                        <div class="price-card-header">
                            <h3 class="price-card-title">Strzyżenie</h3>
                            <span class="price-card-value">100 zł</span>
                        </div>
                        <p class="price-card-duration">Czas trwania: 1 godzina</p>
                    </div>
                    <div class="price-card reveal" data-service-value="Broda">
                        <div class="price-card-header">
                            <h3 class="price-card-title">Pielęgnacja Brody</h3>
                            <span class="price-card-value">80 zł</span>
                        </div>
                        <p class="price-card-duration">Czas trwania: 30 minut</p>
                    </div>
                    <div class="price-card reveal" data-service-value="ComboTrymer">
                        <div class="price-card-header">
                            <h3 class="price-card-title">Combo (Trymer)</h3>
                            <span class="price-card-value">130 zł</span>
                        </div>
                        <p class="price-card-duration">Czas trwania: 1 godzina</p>
                    </div>
                    <div class="price-card reveal" data-service-value="ComboBrzytwa">
                        <div class="price-card-header">
                            <h3 class="price-card-title">Combo (Brzytwa)</h3>
                            <span class="price-card-value">180 zł</span>
                        </div>
                        <p class="price-card-duration">Czas trwania: 1 godzina 30 minut</p>
                    </div>
                </div>
            </div>
        </section>
        
        <!-- Reviews Section -->
        <section id="opinie">
            <div class="container">
                <h2 class="section-title reveal">Zaufali Nam</h2>
                <div class="reviews-content reveal">
                    <div class="rating-summary">
                        <div class="rating-value">5.0</div>
                        <div class="rating-stars">
                            <div class="stars">
                                <svg fill="currentColor" viewBox="0 0 20 20"><path d="M10 15l-5.878 3.09 1.123-6.545L.489 6.91l6.572-.955L10 0l2.939 5.955 6.572.955-4.756 4.635 1.123 6.545z"></path></svg>
                                <svg fill="currentColor" viewBox="0 0 20 20"><path d="M10 15l-5.878 3.09 1.123-6.545L.489 6.91l6.572-.955L10 0l2.939 5.955 6.572.955-4.756 4.635 1.123 6.545z"></path></svg>
                                <svg fill="currentColor" viewBox="0 0 20 20"><path d="M10 15l-5.878 3.09 1.123-6.545L.489 6.91l6.572-.955L10 0l2.939 5.955 6.572.955-4.756 4.635 1.123 6.545z"></path></svg>
                                <svg fill="currentColor" viewBox="0 0 20 20"><path d="M10 15l-5.878 3.09 1.123-6.545L.489 6.91l6.572-.955L10 0l2.939 5.955 6.572.955-4.756 4.635 1.123 6.545z"></path></svg>
                                <svg fill="currentColor" viewBox="0 0 20 20"><path d="M10 15l-5.878 3.09 1.123-6.545L.489 6.91l6.572-.955L10 0l2.939 5.955 6.572.955-4.756 4.635 1.123 6.545z"></path></svg>
                            </div>
                            <span class="rating-count">Na podstawie 179 opinii</span>
                        </div>
                    </div>
                    <p>Nasza reputacja jest dla nas najważniejsza. Dziękujemy za każdą opinię!</p>
                </div>
            </div>
        </section>

        <!-- Booking Section -->
        <section id="rezerwacja">
            <div class="container">
                <h2 class="section-title reveal">Zarezerwuj Online</h2>
                <form class="booking-form reveal" id="bookingForm">
                    <div class="form-group">
                        <label for="service">Usługa</label>
                        <select id="service" name="service" required>
                            <option value="" disabled selected>Wybierz z listy lub kliknij kartę powyżej</option>
                            <option value="Strzyzenie">Strzyżenie (100 zł)</option>
                            <option value="Broda">Pielęgnacja Brody (80 zł)</option>
                            <option value="ComboTrymer">Combo z trymerem (130 zł)</option>
                            <option value="ComboBrzytwa">Combo z brzytwą (180 zł)</option>
                        </select>
                    </div>
                    <div class="form-group">
                        <label for="date">Data</label>
                        <input type="date" id="date" name="date" required>
                    </div>
                    <div class="form-group">
                        <label for="time">Godzina</label>
                        <div id="time-slots">
                            <!-- Time slots generated by JS -->
                        </div>
                         <input type="hidden" id="time" name="time" required>
                    </div>
                    <button type="submit" class="btn" style="width: 100%;">Potwierdź Rezerwację</button>
                </form>
            </div>
        </section>
    </main>

    <!-- Footer Section -->
    <footer class="footer" id="kontakt">
        <div class="container">
            <div class="footer-content reveal">
                <div class="footer-info-grid">
                    <div class="info-block">
                        <h4>Adres</h4>
                        <p>
                            <a href="https://maps.google.com/?q=Ślężna 96, 53-111, Wrocław" target="_blank" rel="noopener noreferrer">
                                Ślężna 96<br>
                                53-111 Wrocław, Krzyki
                            </a>
                        </p>
                    </div>
                    <div class="info-block">
                        <h4>Godziny otwarcia</h4>
                        <p>
                            Pon: 10:00-15:00, 16:00-19:00<br>
                            Wt-Pt: 10:00-15:00, 16:00-20:00<br>
                            Sob: 10:00-15:00<br>
                            Niedz: Zamknięte
                        </p>
                    </div>
                    <div class="info-block">
                        <h4>Social Media</h4>
                        <p>Obserwuj nas na Instagramie!</p>
                        <div class="social-links">
                            <a href="https://www.instagram.com/matesbarbershop" target="_blank" rel="noopener noreferrer" aria-label="Instagram">
                                <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="2" y="2" width="20" height="20" rx="5" ry="5"></rect><path d="M16 11.37A4 4 0 1 1 12.63 8 4 4 0 0 1 16 11.37z"></path><line x1="17.5" y1="6.5" x2="17.51" y2="6.5"></line></svg>
                            </a>
                        </div>
                    </div>
                </div>
                <div class="footer-bottom">
                    <span>&copy; <span id="year"></span> Mates Barber Shop</span>
                    <a href="#" id="privacyPolicyLink">Polityka Prywatności</a>
                </div>
            </div>
        </div>
    </footer>
    
    <!-- Booking Confirmation Modal -->
    <div class="modal-overlay" id="bookingModal">
        <div class="modal-box">
            <h3 id="bookingModalTitle"></h3>
            <p id="bookingModalMessage"></p>
            <button class="btn btn-secondary" data-close-modal>Zamknij</button>
        </div>
    </div>

    <!-- Privacy Policy Modal -->
     <div class="modal-overlay" id="privacyModal">
        <div class="modal-box">
            <h3>Polityka Prywatności</h3>
            <p>
                Zgodnie z art. 13 ust. 1 i 2 rozporządzenia Parlamentu Europejskiego i Rady (UE) 2016/679 z dnia 27 kwietnia 2016 r. w sprawie ochrony osób fizycznych w związku z przetwarzaniem danych osobowych i w sprawie swobodnego przepływu takich danych oraz uchylenia dyrektywy 95/46/WE (ogólne rozporządzenie o ochronie danych), informujemy, że:
                <br><br>
                <strong>1. Administrator Danych Osobowych</strong><br>
                Administratorem Pani/Pana danych osobowych jest: <strong>MATES YEVHENII PYROZHOK</strong>, z siedzibą przy ul. Ślężna 96, 53-111 Wrocław.
                <br><br>
                <strong>2. Cel przetwarzania danych</strong><br>
                Pani/Pana dane osobowe (imię, nazwisko, numer telefonu, adres e-mail) przetwarzane są w celu realizacji usługi rezerwacji wizyty.
                <br><br>
                <strong>3. Podstawa prawna</strong><br>
                Podstawą prawną przetwarzania danych jest Pani/Pana zgoda (art. 6 ust. 1 lit. a RODO) wyrażona poprzez dobrowolne podanie danych w formularzu rezerwacyjnym.
            </p>
            <button class="btn btn-secondary" data-close-modal>Rozumiem</button>
        </div>
    </div>

    <script>
    document.addEventListener('DOMContentLoaded', function() {
        
        // --- SCROLL-TRIGGERED ANIMATIONS ---
        const revealElements = document.querySelectorAll('.reveal');
        const observer = new IntersectionObserver((entries) => {
            entries.forEach(entry => {
                if (entry.isIntersecting) {
                    entry.target.classList.add('visible');
                }
            });
        }, { threshold: 0.1 });
        revealElements.forEach(el => observer.observe(el));

        // --- HEADER SCROLL EFFECT ---
        const header = document.getElementById('header');
        window.addEventListener('scroll', () => {
            header.classList.toggle('scrolled', window.scrollY > 50);
        });

        // --- DYNAMIC YEAR IN FOOTER ---
        document.getElementById('year').textContent = new Date().getFullYear();

        // --- DATE PICKER MINIMUM DATE ---
        const dateInput = document.getElementById('date');
        const today = new Date().toISOString().split('T')[0];
        dateInput.setAttribute('min', today);

        // --- DYNAMIC TIME SLOT GENERATION ---
        const timeSlotsContainer = document.getElementById('time-slots');
        const hiddenTimeInput = document.getElementById('time');
        
        const openingHours = {
            1: [{start: 10, end: 15}, {start: 16, end: 19}], // Monday
            2: [{start: 10, end: 15}, {start: 16, end: 20}], // Tuesday
            3: [{start: 10, end: 15}, {start: 16, end: 20}], // Wednesday
            4: [{start: 10, end: 15}, {start: 16, end: 20}], // Thursday
            5: [{start: 10, end: 15}, {start: 16, end: 20}], // Friday
            6: [{start: 10, end: 15}],                       // Saturday
            0: []                                            // Sunday (Closed)
        };

        function generateTimeSlots(day) {
            const intervals = openingHours[day];
            let slotsHTML = '';

            if (!intervals || intervals.length === 0) {
                timeSlotsContainer.innerHTML = `<div class="closed-message">W tym dniu salon jest zamknięty.</div>`;
                return;
            }

            intervals.forEach(interval => {
                for (let hour = interval.start; hour < interval.end; hour++) {
                    slotsHTML += `<div class="time-slot" data-time="${hour}:00">${hour}:00</div>`;
                    slotsHTML += `<div class="time-slot" data-time="${hour}:30">${hour}:30</div>`;
                }
            });
            
            timeSlotsContainer.innerHTML = slotsHTML;
        }

        dateInput.addEventListener('change', function() {
            const selectedDate = new Date(this.value);
            const dayOfWeek = selectedDate.getUTCDay();
            generateTimeSlots(dayOfWeek);
            hiddenTimeInput.value = '';
        });
        
        // --- TIME SLOT SELECTION LOGIC ---
        timeSlotsContainer.addEventListener('click', function(e) {
            if (e.target.classList.contains('time-slot')) {
                const currentSelected = timeSlotsContainer.querySelector('.selected');
                if (currentSelected) currentSelected.classList.remove('selected');
                e.target.classList.add('selected');
                hiddenTimeInput.value = e.target.dataset.time;
            }
        });

        // --- INTERACTIVE PRICE LIST ---
        const priceCards = document.querySelectorAll('.price-card');
        const serviceSelect = document.getElementById('service');
        priceCards.forEach(card => {
            card.addEventListener('click', () => {
                const serviceValue = card.dataset.serviceValue;
                if (serviceValue) {
                    serviceSelect.value = serviceValue;
                    document.getElementById('rezerwacja').scrollIntoView({ behavior: 'smooth' });
                }
            });
        });
        
        // --- MODAL HANDLING ---
        const bookingModal = document.getElementById('bookingModal');
        const privacyModal = document.getElementById('privacyModal');
        const privacyPolicyLink = document.getElementById('privacyPolicyLink');
        const closeButtons = document.querySelectorAll('[data-close-modal]');

        function showModal(modalElement, title, message) {
            if(title) modalElement.querySelector('h3').textContent = title;
            if(message) modalElement.querySelector('p').innerHTML = message;
            modalElement.classList.add('visible');
        }

        function hideAllModals() {
            document.querySelectorAll('.modal-overlay').forEach(modal => modal.classList.remove('visible'));
        }

        privacyPolicyLink.addEventListener('click', (e) => {
            e.preventDefault();
            showModal(privacyModal);
        });

        closeButtons.forEach(button => button.addEventListener('click', hideAllModals));
        document.querySelectorAll('.modal-overlay').forEach(overlay => {
            overlay.addEventListener('click', (e) => {
                if (e.target === overlay) hideAllModals();
            });
        });

        // --- FORM SUBMISSION ---
        const bookingForm = document.getElementById('bookingForm');
        bookingForm.addEventListener('submit', function(e) {
            e.preventDefault();
            const service = bookingForm.service.options[bookingForm.service.selectedIndex].text;
            const date = bookingForm.date.value;
            const time = bookingForm.time.value;

            if (!time) {
                showModal(bookingModal, "Błąd", "Proszę wybrać dostępną godzinę wizyty.");
                return;
            }

            const confirmationMessage = `
                Twoja rezerwacja została pomyślnie złożona.<br><br>
                <strong>Usługa:</strong> ${service}<br>
                <strong>Data:</strong> ${date}<br>
                <strong>Godzina:</strong> ${time}<br><br>
                To jest tylko demonstracja, żadna rezerwacja nie została zapisana.
            `;
            showModal(bookingModal, "Dziękujemy!", confirmationMessage);
            
            bookingForm.reset();
            timeSlotsContainer.innerHTML = '';
            hiddenTimeInput.value = '';
        });

        // Initial state for time slots
        timeSlotsContainer.innerHTML = `<div class="closed-message">Proszę wybrać datę, aby zobaczyć dostępne godziny.</div>`;
    });
    </script>

</body>
</html>
